---
import Layout from "@/layouts/DashboardPage.astro";
import { getCoursesList } from "@/services/backend/userService";
import { CourseDashboardView } from "@/components/layout/CourseDashboardView";

const metadata = {
  title: "Course | School-MS",
  descriptionMetaTag: "This is meta description",
  pageTitle: "This is Page Title",
};

const courses = await getCoursesList();

console.log("courses", courses);
---

<Layout {...metadata}>
    <CourseDashboardView client:load />
</Layout>
