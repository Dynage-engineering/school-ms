---
import Layout from "@/layouts/DashboardPage.astro";
import { Button } from "@/components/ui/button"
const verificationUrl = import.meta.env.VERIFICATION_URL || "https://schoolms.com/verify";



const metadata = {
	title: "Student | School-MS",
	descriptionMetaTag: "This is meta description",
	pageTitle: "This is Page Title",

};
---

<Layout {...metadata}>
	<main class="flex flex-col items-center justify-center h-full">
		<h1>Student Home Page</h1>
		<a href=`${verificationUrl}` target="_blank">
			<Button> Facial Recognition </Button>
		</a>
	</main>

</Layout>
