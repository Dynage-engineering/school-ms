---
import { UserDashboardView } from "@/components/layout/UserDashboardView";
import Layout from "@/layouts/DashboardPage.astro";

const metadata = {
  title: "Dashboard | School-MS",
  descriptionMetaTag: "This is meta description",
  pageTitle: "This is Page Title",
};
---

<Layout {...metadata}>
  <UserDashboardView client:load />
</Layout>
