---
import Layout from "@/layouts/DashboardPage.astro";
import {Attendance as AttendanceComponent} from "@/components/layout/Attendance";

const metadata = {
  title: "This is title",
  descriptionMetaTag: "This is meta description",
  pageTitle: "This is Page Title",
};
---

<Layout {...metadata}>
  <main class="text-center flex flex-col items-center justify-center h-screen">
    <h1>This is Admin-Only Pages for Attendance Taking class</h1>
    <AttendanceComponent client:load />
  </main>
</Layout>
